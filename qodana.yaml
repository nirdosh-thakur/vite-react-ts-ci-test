version: "1.0"

linter: jetbrains/qodana-js-community:latest

profile:
  name: qodana.recommended

failThreshold: 0

exclude:
  - name: All
    paths:
      - node_modules/**
      - dist/**
      - build/**
      - coverage/**

