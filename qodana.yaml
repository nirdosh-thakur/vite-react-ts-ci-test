version: '1.0'

linter: ghcr.io/jetbrains/qodana-js-community:2024.3

profile:
  name: qodana.recommended

failThreshold: 0

exclude:
  - name: All
    paths:
      - node_modules/**
      - dist/**
      - build/**
      - coverage/**
